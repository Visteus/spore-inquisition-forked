#1
#Finalitas transition, now split into warm-up + entry. The automatic path (lore chain reaching here)
#runs both back-to-back, preserving the original behavior. The offering-gated path runs 17_setup
#early (when the offering is given) so the dimension is already warmed up by the time we get here.
function inqui:lore/17_setup
function inqui:lore/17_enter
